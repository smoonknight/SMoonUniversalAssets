using UnityEngine;

namespace SMoonUniversalAsset
{
    // public static class VectorExtensions
    // {
    //     public static Vector2 PredictJumpDistance(float moveSpeed, float jumpForce, float mass, float gravity, float jumpDuration)
    //     {
    //         float jumpAcceleration = jumpForce / mass;
    //         float jumpVelocity = jumpAcceleration * jumpDuration;

    //         float timeInAir = 2f * jumpVelocity / gravity;
    //         float horizontalDistance = moveSpeed * timeInAir;
    //         float maxHeight = (jumpVelocity * jumpVelocity) / (2f * gravity);

    //         return new Vector2(horizontalDistance, maxHeight);
    //     }
    // }
}